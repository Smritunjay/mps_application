import { DynamicFields } from './dynamic-fields';

describe('DynamicFields', () => {
  it('should create an instance', () => {
    const directive = new DynamicFields();
    expect(directive).toBeTruthy();
  });
});
